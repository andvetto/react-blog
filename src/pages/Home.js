import React from 'react';
import {Typography}  from '@material-ui/core';

export default () => (
    <div className="page-home" style={{ marginTop:28, padding:30 }}>
        <Typography>Welcome Home!</Typography>
    </div>
);